class DesenhosCanvas:

    def __init__(self, canvas):
        self.canvas = canvas

    """def setPixel(self, ponto):
        ponto.pixel = self.canvas.create_line(round(ponto.x), round(ponto.y), round(ponto.x + 1), round(ponto.y + 1), width=0, fill=ponto.cor)
        self.canvas.update()"""

    """def limparTela(self, retas, poligonos, circunferencias, estrutura):
        for x in range(0, len(retas), 1):
            for k in range(0, len(retas[x].pontosReta), 1):
                self.canvas.delete(retas[x].pontosReta[k].pixel)
                self.canvas.update()"""